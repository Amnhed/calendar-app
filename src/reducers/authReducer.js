const initalstate = {
    checking: true,
    //uid: null,
    //name: null
}

export const authReducer = (state = initalstate, action) => {
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}